# bea-l
bea-l is a local Mac-first VSCode helper that emits a plain AI-ready evidence packet from local editor/build context. It does not call model APIs, upload files, mutate source files, or act as an agent.
